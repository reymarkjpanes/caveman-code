Build site: context/plans/build-site-token-efficiency.md
# T-te-005: Snapshot test for byte-stable tool schema SHA256 across 1000 runs
**Status:** done

Test: `packages/ai/src/cache/__tests__/tool-serializer.test.ts` — "produces byte-stable SHA256 across 1000 invocations" iterates `toolSchemaHash()` 1000× and asserts equality to first-run hash.

Runs in ~20ms. Stable.
Acceptance: R2 AC-1 met.
