Build site: context/plans/build-site-token-efficiency.md
# T-te-001: CachePolicy shape + layered breakpoint ordering
**Status:** done

Implementation: `packages/ai/src/cache/policy.ts`

- `CacheLayer` type: `tools|system|project|messages`
- `CACHE_LAYER_ORDER` constant enforces canonical sequence
- `CachePolicy` shape: `{ retention, supportsBreakpoints }`
- `LayerBlock`: `{ layer, bytes, breakpoint? }`
- `validateLayers()` enforces R1: no duplicate layer, canonical order, ≤1 breakpoint per layer
- `CacheRetention = long|short|none` (feeds T-047 per-role retention)

Acceptance:
- R1 / AC-1..AC-4: met via validateLayers + assembleLayered. Non-breakpoint provider accepts same 4-layer payload (covered by layers test).
