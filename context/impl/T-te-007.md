Build site: context/plans/build-site-token-efficiency.md
# T-te-007: Append-only message history invariant
**Status:** done

Implementation: `packages/coding-agent/src/core/append-only-history.ts`

- `AppendOnlyHistory` class — monotonic turn index enforced on append
- Blocks frozen via `Object.freeze` — in-place mutation throws
- `assertPrefixUnchanged()` helper proves prefix integrity between snapshots
- `byteHash(upToTurn?)` exposes cryptographic witness for historical-range integrity

Follow-up: SessionManager can adopt this helper in a later packet; this ships the primitive + contract.

Tests: append-only-history.test.ts (6/6): monotonic append, non-monotonic rejection, frozen-mutation failure, prefix-unchanged, multi-compaction monotonicity, hash-visible integrity.
Acceptance: R4 AC-1, AC-2, AC-3 met.
