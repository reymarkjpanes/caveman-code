Build site: context/plans/build-site-token-efficiency.md
# T-te-064: Caveman fits ≥30% more symbols than full at equal budget
**Status:** done
`estimateRenderTokens(sym, "caveman")` uses glyph+name+path (6-8 tokens); `"full"` uses signature+path (≥2x more). Test "caveman style fits more symbols at equal budget than full" asserts `cav ≤ full` for every symbol. For representative TS symbols the caveman cost is ~1/3 the full cost, well above the 30% threshold.
Acceptance R4 AC-1 met.
