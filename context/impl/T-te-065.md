Build site: context/plans/build-site-token-efficiency.md
# T-te-065: Byte-stable rendering — relative paths, no timestamps, deterministic tie-break
**Status:** done
- `relPath()` strips workdir prefix
- Renderer emits no wall-clock bytes
- PageRank tie-break: file→line→name

Tests: "uses relative path, no absolute workdir", "byte-stable across two renders".
Acceptance R5 AC-1, AC-2, AC-3, AC-4 met.
