Build site: context/plans/build-site-token-efficiency.md
# T-te-070: Inject repomap into project-layer after CLAUDE.md, before user context
**Status:** done
`injectRepomap(projectLayer, repomapBlock)` appends the block after a `<!-- repomap -->` marker. Callers assemble project-layer = `CLAUDE.md contents` + `injectRepomap(...)` + `user-context`, preserving the required ordering.
Tests: inject/strip inverse.
Acceptance R8 AC-1, AC-3 met.
