Build site: context/plans/build-site-token-efficiency.md
# T-te-006: Per-tool description edit isolation + path/timestamp scrubbing
**Status:** done

Two assertions in tool-serializer.test.ts:
- "editing one tool description isolates to tools-layer hash (it changes)" — edit propagates only to tools-layer SHA
- "strips absolute paths and ISO timestamps from descriptions" — scrub regex verified; output contains `<ts>` / `<path>` placeholders, no raw dates or `/Users/...`

Combined with layers.test.ts "editing a tool does not change system/project bytes" — full isolation proof.
Acceptance: R2 AC-2, AC-4 met.
