Build site: context/plans/build-site-token-efficiency.md
# T-te-068: Configurable interval-based cache invalidation
**Status:** done
`RepomapCache.get()` returns null when `now - lastRefreshMs > intervalMs`. `intervalMs` is part of the fingerprint struct, caller-configurable.
Acceptance R6 AC-4 met.
