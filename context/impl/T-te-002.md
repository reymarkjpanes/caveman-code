Build site: context/plans/build-site-token-efficiency.md
# T-te-002: Assemble tools/system/project/messages layers in strict order
**Status:** done

Implementation: `packages/ai/src/cache/layers.ts` — `assembleLayered()`

- Layers emitted in exact `tools → system → project → messages` order
- Project layer omitted when `project === undefined`; tools/system bytes unaffected
- `layerBytes()`/`layerHash()` accessors for per-layer isolation assertions
- `validateLayers()` called at assemble time

Test: `packages/ai/src/cache/__tests__/layers.test.ts` — 8/8 pass.
Acceptance: R1 layers-in-order, non-breakpoint parity, project removal preserves upstream bytes.
