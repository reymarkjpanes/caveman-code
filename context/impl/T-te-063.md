Build site: context/plans/build-site-token-efficiency.md
# T-te-063: Caveman-style renderer + full-prose toggle
**Status:** done
`renderRepomap(symbols, {style, workdir})`:
- `caveman`: `fn foo@a.ts:12` (glyph + name@path:line)
- `full`: `signature — path:line`

Test: "full style produces human-readable signature + path", "byte-stable across two renders".
Acceptance R4 AC-2, AC-3 met.
