Build site: context/plans/build-site-token-efficiency.md
# T-te-003: Single breakpoint per layer + layer-isolation byte stability
**Status:** done

`validateLayers()` in `packages/ai/src/cache/policy.ts`:
- Rejects duplicate layer (test: "rejects duplicate layer")
- Rejects out-of-canonical-order layers (test: "rejects out-of-order layers")
- Counts breakpoints per layer, throws if >1

Layer isolation test ("editing a tool does not change system/project bytes") proves tool-layer hash diverges while system/project hashes remain identical — byte stability per layer confirmed.

Acceptance: R1 all AC met.
