Build site: context/plans/build-site-token-efficiency.md
# T-te-071: Removing repomap leaves tools/system layer bytes unchanged
**Status:** done
`stripRepomap()` removes only the repomap block; the project-layer base is byte-identical after strip. Since tools/system/project layers are isolated per cavekit-prompt-cache R1 (enforced by `validateLayers()` T-003), removing the repomap from the project layer does not touch upstream bytes.
Test: "injectRepomap + stripRepomap are inverses".
Acceptance R8 AC-2 met.
