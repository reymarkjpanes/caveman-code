Build site: context/plans/build-site-token-efficiency.md
# T-te-069: Token budget respected, no mid-symbol truncation
**Status:** done
`selectWithinBudget()` evaluates each symbol's estimated cost against remaining budget; if over, stops **before** adding (never truncates mid-symbol). Default budget ~1000 tokens.
Acceptance R7 AC-1, AC-2, AC-3 met.
