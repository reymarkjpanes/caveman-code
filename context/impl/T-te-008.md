Build site: context/plans/build-site-token-efficiency.md
# T-te-008: 20-turn compaction immutability integration test
**Status:** done

Test case: "20-turn compaction leaves pre-15 bytes unchanged" in append-only-history.test.ts.

- Seed 20 turns
- Capture `byteHash(14)` and full snapshot before compaction
- Call `compactTo(14, summary)` → appends summary at turn 20
- Assert `byteHash(14)` unchanged
- Assert `assertPrefixUnchanged(before, after, 15)` passes

Green.
Acceptance: R4 AC-1 fully verified.
