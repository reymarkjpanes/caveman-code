Build site: context/plans/build-site-token-efficiency.md
# T-te-067: Session-scoped incremental refresh cache keyed on HEAD + mtime
**Status:** done
`packages/agent/src/repomap/cache.ts` — `RepomapCache` with `get/put/isStale/invalidate`. Fingerprint = `{ gitHead, mtimes: Map, intervalMs, lastRefreshMs }`. `isStale` returns true on HEAD change or any mtime delta.
Tests: HEAD-stale, mtime-stale, fresh-hit.
Acceptance R6 AC-1, AC-2, AC-3 met.
